# Minimal TypeScript connector

A tiny Sophon connector that echoes user messages back into iOS. Use it to verify pairing, WebSocket updates, streaming deltas, and finalization before wiring a real runtime.

```sh
cp .env.example .env
npm install
npm run dev
```
